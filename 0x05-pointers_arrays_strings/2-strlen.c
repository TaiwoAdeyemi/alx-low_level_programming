#include "main.h"
#include <stdio.h>
/**
 * _strlen - the lenght of a string.
 *@s: char pointer.
 *Return: lenght of a string.
 */

int _strlen(char *s)
{
	int lenght = 0;

	while (*s != '\0')
	{
		length++;
		s++;
	}

	return length;
}
